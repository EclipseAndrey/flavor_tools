name: flavor_tools
description: CLI tool for creating and managing Flutter flavors. Configures iOS (Xcode, xcconfig, schemes) and Android (Gradle) automatically.
version: 2.0.3
repository: https://github.com/EclipseAndrey/flavor_tools

environment:
  sdk: ^3.5.0

# Add regular dependencies here.
dependencies:
  # path: ^1.8.0
  xcode_parser: ^2.0.0
  args: ^2.7.0
  xml: ^6.6.1
  collection: ^1.19.1
  yaml: ^3.1.3

dev_dependencies:
  lints: ^6.1.0
  test: ^1.30.0
